package multiplefile;

/* This is basically the minimal example of a program with a type error, across two files. */
public class Main {
    public static void main(String[] args) {
        Util.parameterShouldBeNullable(null);
    }
}
